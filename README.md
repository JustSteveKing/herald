# herald

Send fake, correctly signed provider webhooks to any URL.

Pick Stripe, pick `payment_intent.succeeded`, point it at
`http://localhost:8000/webhooks`, send. The signature verifies.

Work in progress.
